spring.application.name=discovery-service

# 8761 is the Eureka convention; clients default to looking here
server.port=8761

# This app IS the registry, so it should NOT register with itself,
# nor download a copy of the registry from itself. Turn both off.
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
